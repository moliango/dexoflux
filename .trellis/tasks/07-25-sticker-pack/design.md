# Design — sticker pack
- External market `https://s.pwsh.us.kg` via `StickerMarketStore`
- UIKit panel: emoji tab reuses `EmojiPickerView`, sticker tab + market sheet
- Insert markdown `![name|WxH,30%](url)` into composers
- Configurable base URL with cache clear
